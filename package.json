{
  "name": "project-v-rage-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-native RAGE compatibility runtime foundation for user-supplied Xbox 360 GTA V data.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check apps/engine-lab/main.js && node --check packages/vfs/src/index.js && node --check packages/rpf/src/index.js && node --check packages/rage-formats/src/index.js && node --check packages/streaming/src/index.js && node --check packages/world/src/index.js && node --check packages/wasm-bridge/src/index.js",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
