# Game runtime shell

The playable app will be reconnected after the asset pipeline can stream real world sectors. Temporary Michael/Meshy files are not runtime dependencies anymore.

The game app should depend on normalized world/mesh/texture/collision data, never directly on RPF implementation details.
